export enum ProductType {
    CLASS = 'CLASS',
    RESOURCE = 'RESOURCE'
}


export enum OrderStatus {
    PENDING = 'PENDING',
    PAID = 'PAID',
    FAILED = 'FAILED',
    CANCELLED = 'CANCELLED'
}


export enum SeatReservation {
    ACTIVE = 'ACTIVE',
    CONFIRMED = 'CONFIRMED',
    EXPIRED = 'EXPIRED',
}